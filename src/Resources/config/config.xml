<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/shopware/platform/master/src/Core/System/SystemConfig/Schema/config.xsd">

    <card>
        <title>Basic Configuration</title>

        
        <input-field type="text">
            <name>defaultTax</name>
            <label>Default Tax Id</label>
            <helpText>Enter the Id of the tax that can be used when the tax is null.</helpText>
        </input-field>

        <input-field type="text">
            <name>defaultManufacturer</name>
            <label>Default Manufacturer Id</label>
            <helpText>Enter the Id of the manufacturer that can be used when the manufacturer is missing.</helpText>
        </input-field>

        <input-field type="text">
            <name>defaultCustomerGroup</name>
            <label>Default Customer Group Id</label>
            <helpText>Enter the Id of the customer group to use when a customer's group is missing or unmapped.</helpText>
        </input-field>
    </card>
</config>